# Secure Chat GUI (Fixed Version)
Steps to Run:
1. cd secure-chat-fixed
2. python -m venv venv
3. venv\Scripts\activate
4. pip install -r requirements.txt
5. Run server: python server/server.py
6. Run clients: python client/client_alice_gui.py and client/client_bob_gui.py in separate terminals.
